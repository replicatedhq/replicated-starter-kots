#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

echo "Linting Helm chart..."
helm lint "${PROJECT_ROOT}/charts/app"

echo "Helm chart lint passed."
